import React, { Component, PropTypes } from 'react'

class Home extends Component {
  render() {
    return (
      <div>
        Chat...
      </div>
    );
  }
}

Home.PropTypes = {
  location: PropTypes.object,
}

export default Home;